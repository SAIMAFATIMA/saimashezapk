import { Heading, Page, Button } from "@shopify/polaris";

const Index = () => (
  <Page>
  <Button>create a template</Button>
  <Button>edit the template</Button>
  <Button>save the template</Button>
  </Page>
);

export default Index;
